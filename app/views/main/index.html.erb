<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
</head>
    <body>
        <h1>Ruby on rails and PostgresSQL</h1>
        <ul>
            <% @articles.each do |article| %>
                <li>
                    <a href="/main/<%= article.id %>">
                        <%= article.title %>
                    </a>
                    <span>
                        <%= article.body %>
                    </span>
                    <span>
                        <%= article.id %>
                    </span>
                    <input type="datetime" value="<%= article.created_at %>" />
                    <input type="datetime" value="<%= article.updated_at %>" />
                </li>
            <% end %>
        </ul>


        <div class="container">
        <% @numbers_array.each do |number| %>
            <div class="item">
                <%= number %>
            </div>
        <% end %>
        </div>


    </body>
</html>
